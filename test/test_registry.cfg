<?xml version="1.0"?>
<!-- Teaches cppcheck the registration macros in test_registry.hpp, which it
     does not read: the lint target passes it no include path. -->
<def format="2">
  <define name="TEST(name)" value="void name()"/>
  <define name="TEST_IN(suite, name)" value="void name()"/>
</def>
